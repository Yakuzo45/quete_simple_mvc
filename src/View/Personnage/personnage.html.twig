{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Personnages</h1>
    <ul>
        {% for personnage in personnages %}
            <li><a href="/item/{{ item.id }}">{{ item.id }}. {{ item.title }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/item/add">Ajouter</a>
{% endblock %}
